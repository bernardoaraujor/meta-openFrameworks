This README file contains information on the contents of the
meta-openFrameworks layer.

Please see the corresponding sections below for details.


Dependencies
============

This layer depends on:

  URI: git://git.openembedded.org/meta-openembedded
  layers: meta-oe, meta-multimedia
  branch: master

  URI: http://git.yoctoproject.org/cgit/cgit.cgi/poky
  layers: meta, meta-poky, meta-yocto-bsp
  branch: master

Patches
=======

Please submit any patches against the meta-openFrameworks layer to the maintainer:

Bernardo A. Rodrigues <bernardoar@protonmail.com>


Table of Contents
=================

  I. Adding the meta-openFrameworks layer to your build
 II. Misc


I. Adding the meta-openFrameworks layer to your build
=================================================

--- replace with specific instructions for the meta-openFrameworks layer ---

In order to use this layer, you need to make the build system aware of
it.

Assuming the meta-openFrameworks layer exists at the top-level of your
yocto build tree, you can add it to the build system by adding the
location of the meta-openFrameworks layer to bblayers.conf, along with any
other layers needed. e.g.:

  BBLAYERS ?= " \
    /path/to/yocto/meta \
    /path/to/yocto/meta-poky \
    /path/to/yocto/meta-yocto-bsp \
    /path/to/yocto/meta-meta-openFrameworks \
    "


II. Misc
========

--- replace with specific information about the meta-openFrameworks layer ---
